import { ExpandCollapseAllDirective } from './expand-collapse-all.directive';

describe('ExpandCollapseAllDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandCollapseAllDirective();
    expect(directive).toBeTruthy();
  });
});
